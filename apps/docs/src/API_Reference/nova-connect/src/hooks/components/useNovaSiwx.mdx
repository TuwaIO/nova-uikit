[**@tuwaio/nova-uikit**](?path=/docs/api-reference-readme--docs)

***

# useNovaSiwx()

> **useNovaSiwx**(`options?`): `object`

Defined in: [packages/nova-connect/src/hooks/useNovaSiwx.ts:31](https://github.com/TuwaIO/nova-uikit/blob/a2fec1546b58c024e38b90bc843fd1315e411402/packages/nova-connect/src/hooks/useNovaSiwx.ts#L31)

React hook that exposes SIWX authentication controls within NovaConnect applications.

## Parameters

### options?

[`UseNovaSiwxOptions`](?path=/docs/api-reference-nova-connect-src-hooks-interfaces-usenovasiwxoptions--docs)

Default SIWX field options and verifier callback.

## Returns

`object`

Object containing `signIn` and `signOut` handlers.

### signIn

> **signIn**: (`overrideConnection?`, `customVerifier?`) => `Promise`\<`void`\> = `handleSignIn`

#### Parameters

##### overrideConnection?

`Connector`\<[`EVMConnection`](?path=/docs/api-reference-nova-connect-src-evm-interfaces-evmconnection--docs) \| [`SolanaConnection`](?path=/docs/api-reference-nova-connect-src-solana-interfaces-solanaconnection--docs)\>

##### customVerifier?

(`payload`) => `Promise`\<`SiwxClientSession` \| `null`\>

#### Returns

`Promise`\<`void`\>

### signOut

> **signOut**: () => `void`

#### Returns

`void`

## Example

```tsx
const { signIn, signOut } = useNovaSiwx({ verifier: myVerifier });
await signIn();
```
