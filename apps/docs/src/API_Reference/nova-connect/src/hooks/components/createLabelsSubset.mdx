[**@tuwaio/nova-uikit**](../../../../README.md)

***

# createLabelsSubset()

> **createLabelsSubset**\<`K`\>(`labels`, `keys`): `Pick`\<`NovaConnectLabels`, `K`\>

Defined in: [packages/nova-connect/src/hooks/useNovaConnectLabels.ts:227](https://github.com/TuwaIO/nova-uikit/blob/3b601525e0a7def91d7ef8a4b0fa6992a1c63862/packages/nova-connect/src/hooks/useNovaConnectLabels.ts#L227)

Utility function to create a labels subset (for use outside of React components)

## Type Parameters

### K

`K` *extends* keyof `NovaConnectLabels`

## Parameters

### labels

`NovaConnectLabels`

The source labels object

### keys

`K`[]

Array of keys to extract

## Returns

`Pick`\<`NovaConnectLabels`, `K`\>

Object with only the requested labels

## Example

```typescript
const actionLabels = createLabelsSubset(labels, ['connect', 'disconnect', 'tryAgain']);
```
