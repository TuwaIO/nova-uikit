[**@tuwaio/nova-uikit**](../../../../README.md)

***

# useNovaConnectLabelsSubset()

> **useNovaConnectLabelsSubset**\<`K`\>(`keys`): `Pick`\<`NovaConnectLabels`, `K`\>

Defined in: [packages/nova-connect/src/hooks/useNovaConnectLabels.ts:98](https://github.com/TuwaIO/nova-uikit/blob/5a93697fcd0b89689a6ed324b8d467f45bbc2e7a/packages/nova-connect/src/hooks/useNovaConnectLabels.ts#L98)

Hook to get multiple specific labels for better performance

## Type Parameters

### K

`K` *extends* keyof `NovaConnectLabels`

## Parameters

### keys

`K`[]

Array of label keys to retrieve

## Returns

`Pick`\<`NovaConnectLabels`, `K`\>

Object with only the requested labels

## Example

```typescript
function ConnectModal() {
  const { connectWallet, connecting, disconnect } = useNovaConnectLabelsSubset(['connectWallet', 'connecting', 'disconnect']);

  return (
    <div>
      <h2>{connectWallet}</h2>
      <span>{connecting}</span>
      <button>{disconnect}</button>
    </div>
  );
}
```
