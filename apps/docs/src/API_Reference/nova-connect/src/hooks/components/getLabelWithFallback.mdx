[**@tuwaio/nova-uikit**](?path=/docs/api-reference-readme--docs)

***

# getLabelWithFallback()

> **getLabelWithFallback**(`labels`, `key`, `fallback?`): `string`

Defined in: [packages/nova-connect/src/hooks/useNovaConnectLabels.ts:203](https://github.com/TuwaIO/nova-uikit/blob/e4ce53b175c2115006bc90676324e32d34e9cd5b/packages/nova-connect/src/hooks/useNovaConnectLabels.ts#L203)

Utility function to get a formatted label with fallback

## Parameters

### labels

`NovaConnectLabels`

The labels object

### key

keyof `NovaConnectLabels`

The label key

### fallback?

`string`

Optional fallback text

## Returns

`string`

The label value or fallback

## Example

```typescript
const buttonText = getLabelWithFallback(labels, 'connectWallet', 'Connect');
```
