[**@tuwaio/nova-uikit**](../../../../README.md)

***

# isEvmChainListSync()

> **isEvmChainListSync**(`chains`): `boolean`

Defined in: [packages/nova-connect/src/utils/getChainsListByConnectorType.ts:292](https://github.com/TuwaIO/nova-uikit/blob/5a93697fcd0b89689a6ed324b8d467f45bbc2e7a/packages/nova-connect/src/utils/getChainsListByConnectorType.ts#L292)

Synchronous EVM chain list validation using pre-loaded adapters.

Provides immediate validation results without triggering adapter loading.
Falls back to basic type checking if adapter isn't pre-loaded.

## Parameters

### chains

(`string` \| `number`)[]

Array of chain identifiers to validate

## Returns

`boolean`

True if chains are valid for EVM

## Example

```typescript
const isEvm = isEvmChainListSync([1, 137, 56]); // true (immediate result)
```

## Since

1.0.0
