[**@tuwaio/nova-uikit**](../../../../README.md)

***

# getAdapterStatus()

> **getAdapterStatus**(`type`): [`AdapterLoadStatus`](../type-aliases/AdapterLoadStatus.md)

Defined in: [packages/nova-connect/src/utils/getChainsListByConnectorType.ts:512](https://github.com/TuwaIO/nova-uikit/blob/549de94c179623155df073fefe76400fcd65bea1/packages/nova-connect/src/utils/getChainsListByConnectorType.ts#L512)

Gets the current loading status of a specific adapter.

Returns the current state of an adapter in the loading lifecycle. Useful
for UI indicators, debugging, or conditional logic based on adapter
availability.

## Parameters

### type

`OrbitAdapter`

The adapter type to check

## Returns

[`AdapterLoadStatus`](../type-aliases/AdapterLoadStatus.md)

Current loading status of the adapter

## Example

```typescript
const evmStatus = getAdapterStatus(OrbitAdapter.EVM);

switch (evmStatus) {
  case 'idle':
    console.log('EVM adapter not yet requested');
    break;
  case 'loading':
    // Show loading spinner
    break;
  case 'loaded':
    // Enable EVM features
    break;
  case 'error':
    // Show fallback UI
    break;
}
```

## Since

1.0.0
