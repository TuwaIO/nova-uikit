[**@tuwaio/nova-uikit**](../../../README.md)

***

# applyFirstPathFill()

> **applyFirstPathFill**(`svgString`, `fillColor`): `string`

Defined in: [packages/nova-core/src/utils/svgUtils.ts:32](https://github.com/TuwaIO/nova-uikit/blob/c6264598fe8e3f231a0d8884c06f8c0b311f3334/packages/nova-core/src/utils/svgUtils.ts#L32)

Modifies the fill attribute of the first `<path>` element in an SVG string.
Uses DOMParser for safe XML manipulation.
Automatically resolves CSS variables (e.g., `var(--color)`) to their computed values.

## Parameters

### svgString

`string`

The original SVG markup

### fillColor

`string`

The color to apply (supports CSS variables)

## Returns

`string`

Modified SVG string
