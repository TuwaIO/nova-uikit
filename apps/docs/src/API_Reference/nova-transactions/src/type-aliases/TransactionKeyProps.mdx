[**@tuwaio/nova-uikit**](../../../README.md)

***

# TransactionKeyProps\<T\>

> **TransactionKeyProps**\<`T`\> = `Pick`\<[`NovaProviderProps`](../providers/type-aliases/NovaProviderProps.md)\<`T`\>, `"adapter"`\> & `object`

Defined in: [nova-transactions/src/components/TransactionKey.tsx:20](https://github.com/TuwaIO/nova-uikit/blob/bb57d95d18f8de8b14211d55bcd6b5765a378a6e/packages/nova-transactions/src/components/TransactionKey.tsx#L20)

Defines the props for the TransactionKey component.

## Type Declaration

### className?

> `optional` **className**: `string`

Optional additional CSS classes for the container.

### confirmations?

> `optional` **confirmations**: `number`

Optional number of confirmations for a transaction.

### renderHashLink()?

> `optional` **renderHashLink**: (`props`) => `ReactNode`

An optional render prop to allow for complete customization of how the hash link is rendered.
If not provided, the default `HashLink` component will be used.

#### Parameters

##### props

[`HashLinkProps`](HashLinkProps.md)

#### Returns

`ReactNode`

### tx

> **tx**: `T`

The transaction object to display identifiers for.

### variant?

> `optional` **variant**: `"toast"` \| `"history"`

The visual variant, which applies different container styles.

## Type Parameters

### T

`T` *extends* `Transaction`

The transaction type.

## Template

The type of the tracker identifier.

## Template

The type of the key returned by an action function.
